#!/usr/bin/env python3
"""Generate detailed DB notes - v2 expanded."""

import os, re

OUT = r'E:\Claudecode\personal-site\notes-prep'

def w(filename, content):
    path = os.path.join(OUT, filename)
    with open(path, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f'  OK {filename}')

SVG_SCHEMA = '''<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 580 200" width="100%" height="200">
  <rect x="20" y="10" rx="5" ry="5" width="540" height="40" fill="#e3f2fd" stroke="#1976d2" stroke-width="1.5"/>
  <text x="160" y="34" text-anchor="middle" font-size="12" fill="#1565c0" font-weight="bold">外模式（用户视图）</text>
  <text x="370" y="34" text-anchor="middle" font-size="11" fill="#555">用户的局部数据视图</text>
  <line x1="290" y1="50" x2="290" y2="65" stroke="#555" stroke-width="1.5" marker-end="url(#dsa)"/>
  <text x="300" y="62" font-size="10" fill="#e65100">外模式/模式映象 → 逻辑独立性</text>
  <rect x="20" y="70" rx="5" ry="5" width="540" height="40" fill="#e8f5e9" stroke="#43a047" stroke-width="1.5"/>
  <text x="160" y="94" text-anchor="middle" font-size="12" fill="#2e7d32" font-weight="bold">模式（逻辑视图）</text>
  <text x="370" y="94" text-anchor="middle" font-size="11" fill="#555">全体数据的逻辑结构</text>
  <line x1="290" y1="110" x2="290" y2="125" stroke="#555" stroke-width="1.5" marker-end="url(#dsa)"/>
  <text x="300" y="122" font-size="10" fill="#e65100">模式/内模式映象 → 物理独立性</text>
  <rect x="20" y="130" rx="5" ry="5" width="540" height="40" fill="#fff3e0" stroke="#e65100" stroke-width="1.5"/>
  <text x="160" y="154" text-anchor="middle" font-size="12" fill="#e65100" font-weight="bold">内模式（存储视图）</text>
  <text x="370" y="154" text-anchor="middle" font-size="11" fill="#555">数据的物理存储方式</text>
  <defs><marker id="dsa" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto"><path d="M 0 0 L 10 5 L 0 10 z" fill="#555"/></marker></defs>
</svg>'''

def db_00():
    return '''# 数据库系统原理 · 复习笔记目录

## 学习导引

数据库系统原理课程的核心是学习如何有效地组织、存储和管理数据。课程内容可以大致分为三个层面：首先是理论基础（关系代数、范式理论），其次是实践工具（SQL语言），最后是设计方法（E-R模型、数据库设计流程）。这三个层面相互支撑——理论基础指导设计，设计的结果通过SQL来实现。

本系列笔记按照从基础到应用的顺序编排，每个章节都配有典型例题和详细解析。建议先掌握关系代数和SQL语言（考试重点），再深入学习范式理论和数据库设计。

## 章节总览

| 章节 | 核心内容 | 考试权重 |
|------|---------|---------|
| **DB-01 绪论** | 数据库系统基本概念、三级模式、数据独立性 | ⭐⭐ |
| **DB-02 关系数据库** | 关系模型、完整性约束、关系代数运算 | ⭐⭐⭐⭐ |
| **DB-03 SQL语言** | DDL/DML/DQL、连接查询、嵌套查询、视图、授权 | ⭐⭐⭐⭐⭐ |
| **DB-04 安全性与完整性** | 存取控制、触发器、约束定义 | ⭐⭐ |
| **DB-05 关系数据理论** | 函数依赖、范式、闭包算法、最小依赖集 | ⭐⭐⭐⭐⭐ |
| **DB-06 数据库设计** | 设计阶段划分、E-R图转关系模式 | ⭐⭐⭐ |

## 考试重点提醒

- 关系代数表达式书写（每年必考一道大题）
- SQL语句书写（特别是连接查询、嵌套查询、GROUP BY分组查询）
- 范式判定（给定函数依赖集，判断属于第几范式，并给出分解方案）
- 闭包计算和候选码求解
- E-R图设计及向关系模式的转换
'''


def db_01():
    return f'''# DB-01 绪论

## 一、数据库系统的基本概念

学习数据库系统首先要理清四个容易混淆的概念：数据、数据库、数据库管理系统和数据库系统。

**数据**是数据库中存储的基本对象。数据不仅仅是数字，文字、图形、图像、音频、视频等都是数据的不同表现形式。数据的含义称为"语义"，数据本身需要结合语义才有意义。例如，数字"35"本身没有意义，但如果知道它的语义是"某个学生的年龄"，它就变成了有价值的信息。

**数据库**是长期存储在计算机内、有组织、可共享的大量数据集合。数据库具有三个特征：永久存储（数据不会因为程序结束而消失）、有组织（数据按照一定的结构存放）、可共享（多个用户可以同时访问同一份数据）。这与文件系统中的数据文件有本质区别。

**数据库管理系统**是位于用户与操作系统之间的数据管理软件。DBMS的核心功能包括数据定义（提供DDL语言让用户定义数据结构）、数据操纵（提供DML语言让用户增删改查数据）、数据组织（决定数据如何存储在磁盘上）、事务管理（保证并发访问的正确性和故障恢复能力）等。

**数据库系统**是一个更大的概念，它包含了数据库、DBMS、应用程序、数据库管理员和用户。当我们在说"我们使用了Oracle数据库"时，通常指的是数据库系统。

## 二、数据管理技术的发展

数据管理经历了三个重要的阶段：

**人工管理阶段**（20世纪50年代以前）：计算机主要用于科学计算，没有专门的软件管理数据。程序员不仅要编写程序逻辑，还要自己管理数据的存储位置和格式。一组数据只对应一个程序，数据不能共享。

**文件系统阶段**（20世纪50-60年代）：出现了磁带、磁盘等存储设备，也有了专门的文件管理系统。程序通过文件系统来读写数据，但数据文件之间缺乏联系，数据冗余度高，且数据独立性差。

**数据库系统阶段**（20世纪60年代末至今）：数据库技术的发展解决了文件系统的种种弊端。在这个阶段，数据实现了结构化存储，不同应用程序可以共享同一份数据，数据的逻辑结构和物理存储相互独立，DBMS提供了并发控制、安全控制、完整性检查等高级功能。

数据库系统阶段的核心特点可以概括为"三高一低"：高结构化（数据有统一的组织方式）、高共享低冗余（同一份数据可以被多个应用使用，重复存储减少）、高数据独立性（应用程序不依赖数据的物理存储方式）。

## 三、数据独立性

{SVG_SCHEMA}

数据独立性是数据库系统区别于文件系统的重要特征。它分为两个层面。

**物理独立性**是指，当数据的物理存储结构改变时（例如更换了硬盘、改变了文件的存放位置、调整了存储块的大小），应用程序不需要修改。这是因为DBMS的"模式/内模式映象"负责将逻辑结构映射到物理存储，只要修改这个映射关系，就能保证模式层不变，从而应用程序无需变化。

**逻辑独立性**是指，当数据库的逻辑结构改变时（例如增加了一个新字段、修改了某个表的结构），应用程序不需要修改。这是因为"外模式/模式映象"机制将用户视图与全局逻辑结构分开。DBA只需要修改外模式与模式之间的映射，就能保证用户程序看到的仍然是原来的结构。

从实现的角度来看，数据独立性是通过数据库系统的三级模式（外模式、模式、内模式）和两级映像（外模式/模式映像、模式/内模式映像）来实现的。

## 四、数据模型

数据模型是对现实世界数据特征的抽象。它通常由三部分组成：数据结构（描述数据的类型和联系）、数据操作（允许执行的操作类型）和完整性约束（数据必须满足的条件）。

按照抽象层次的不同，数据模型分为：
- **概念模型**（如E-R模型）：按用户的视角来建模，独立于具体的DBMS
- **逻辑模型**（如关系模型、层次模型、网状模型）：按计算机的视角来建模，是DBMS支持的数据组织方式
- **物理模型**：描述数据在存储介质上的组织方式

当前最主流的逻辑模型是关系模型，它将数据组织为一张张二维表。

## 五、E-R模型基础

E-R模型（实体-联系模型）是概念设计阶段最常用的工具。它的三个基本元素是：

**实体型**用矩形表示，如"学生"、"课程"。实体是客观存在并可相互区分的事物。

**属性**用椭圆形表示，描述实体的特征。如学生的"学号"、"姓名"、"年龄"等。

**联系**用菱形表示，描述实体之间的关联。联系有三种类型：一对一（1:1）、一对多（1:n）和多对多（m:n）。例如，一个班级有一名班主任（1:1），一个班级有多名学生（1:n），一门课程有多名学生选修、一个学生选修多门课程（m:n）。

## 六、典型例题

**例题1**：判断对错：数据库管理系统的核心功能是数据查询。

> 错误。数据查询只是数据操纵功能的一部分。DBMS的核心功能包括数据定义、数据组织存储、数据操纵（含查询）、运行管理和数据库维护五大方面，缺一不可。

**例题2**：企业的数据库进行了两项操作：①将数据从机械硬盘迁移到SSD；②为核心表增加了两个新字段。两项操作均未影响前端业务程序运行。请分析这两个现象对应数据库的什么特性。

> 解答：
> ① 硬盘更换属于物理存储的改变——对应**物理独立性**。DBMS通过修改模式/内模式映像来保证模式层不变，应用程序不受影响。
> ② 增加字段属于表结构的改变（逻辑结构变化）——对应**逻辑独立性**。DBMS通过修改外模式/模式映像来保证用户视图不变，应用程序无须修改。
'''

def db_02():
    return '''# DB-02 关系数据库

## 一、关系模型的基本概念

关系模型是E.F.Codd在1970年提出的，它用"关系"（即二维表）来表示数据和数据之间的联系。关系模型之所以成为主流，是因为它有坚实的数学基础（集合论和谓词逻辑）和简洁统一的数据结构。

**关系**就是一张二维表。表中的每一行称为一个"元组"（对应一条记录），每一列称为一个"属性"（对应一个字段）。每个属性有一个取值范围，称为"域"。

**候选码**是能唯一标识一个元组的最小属性集合。例如，"学生（学号，姓名，身份证号，班级）"这个关系中，"学号"和"身份证号"都可以唯一确定一个学生，且都不能再缩小，所以它们都是候选码。

**主码**是从候选码中选定一个作为主要标识的码。通常在表设计中选择最常用的候选码作为主码。

**外码**是表中的一个或一组属性，它引用另一个表的主码。外码用于建立表与表之间的关联。例如，"学生"表中的"班级编号"属性引用了"班级"表的主码"班级编号"，那么"班级编号"就是学生表的外码。

## 二、三类完整性约束

完整性约束是保证数据库中数据正确性和一致性的规则。关系模型定义了三类完整性约束。

**实体完整性**：主码的值不能取空值（NULL）。这个规则很容易理解——如果主码为空，就无法唯一确定一个元组，关系就失去了完整性。例如，"学生"表的主码是"学号"，那么在插入学生记录时，学号字段不能为空。

**参照完整性**：外码的值要么为空，要么必须等于被引用表中某个元组的主码值。这个规则保证了表之间的关联不会"断裂"。例如，如果一个学生的"班级编号"字段值是"2024级软工1班"，那么在"班级"表中必须存在一个主码为"2024级软工1班"的元组。

**用户定义完整性**：针对某个具体应用场景的约束条件。例如，年龄必须在0-150之间，性别只能取"男"或"女"，成绩必须在0-100之间等。这些约束反映了业务规则对数据的要求。

## 三、关系代数运算

关系代数是一组作用于关系的运算，每个运算的输入是一个或多个关系，输出是一个新的关系。关系代数是SQL语言的数学基础。

### 3.1 传统集合运算

并（Union）、交（Intersection）、差（Difference）要求两个关系必须"兼容"——即属性数量相同，且对应属性取自同一个域。

**并运算 R U S**：返回在R或S中出现的所有元组。

**交运算 R ∩ S**：返回同时在R和S中出现的元组。

**差运算 R - S**：返回在R中出现但不在S中出现的元组。

**广义笛卡尔积 R × S**：R中的每个元组与S中的每个元组进行拼接，结果是一个包含R和S所有属性的新关系，元组数为|R| × |S|。

### 3.2 专门关系运算

**选择（Selection）**：用σ表示，从关系中选取满足条件的行（元组）。这是对行（水平方向）的操作。例如，σ(Sdept='CS')(Student)表示从学生表中选取所有计算机系的学生。

**投影（Projection）**：用π表示，从关系中选取指定的列（属性）。这是对列（垂直方向）的操作。投影后可能减少元组数量，因为重复的行会被去掉。例如，π(Sname, Sage)(Student)从学生表中选取姓名和年龄两列。

**连接（Join）**：用⋈表示，将两个关系按指定的条件组合成一个新的关系。其中最常用的是等值连接（按相等条件连接）和自然连接（在等值连接的基础上去除重复列）。自然连接要求两个关系中有同名的属性列。

**除（Division）**：用÷表示，是关系代数中最复杂的运算。R ÷ S的结果是满足条件的X属性值——这些X的象集必须包含S在Y上的所有投影。

### 3.3 综合查询例题

设有关系模式：
```
Student(Sno, Sname, Ssex, Sage, Sdept)
Course(Cno, Cname, Cpno, Credit)
SC(Sno, Cno, Grade)
```

**例1**：查询选修了2号课程的学生姓名。
> π_Sname(σ_Cno='2'(SC ⋈ Student))
> 先对SC做选择（筛选出课程号为2的记录），然后与Student做自然连接，最后投影出姓名。

**例2**：查询选修了所有课程的学生学号。
> π_Sno,Cno(SC) ÷ π_Cno(Course)
> 先求SC在Sno和Cno上的投影（每个学生选了哪些课），再求Course在Cno上的投影（全部课程的列表），两者相除得到"选了所有课程"的学生学号。

**例3**：查询没有任何一门课程不及格的学生学号和姓名。
> π_Sno,Sname(Student) - π_Sno,Sname(σ_Grade<60(Student ⋈ SC))
> 全体的学生减去至少有一门不及格的学生。

**例4**：查询选修了数据库原理课程的学生姓名。
> π_Sname(σ_Cname='数据库原理'(Course ⋈ SC ⋈ Student))

**例5**：查询至少选修了95001选修的所有课程的学生学号。
> 设K = π_Cno(σ_Sno='95001'(SC))
> 结果 = π_Sno,Cno(SC) ÷ K
''';

def db_03():
    return '''# DB-03 SQL语言

## 一、SQL概述

SQL（Structured Query Language）是关系数据库的标准语言。它集数据定义、数据操纵、数据查询和数据控制功能于一体，是一种"全功能"的数据库语言。SQL语言非常简洁——核心命令只有九个单词（SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, DROP, GRANT, REVOKE），但能完成几乎所有数据库操作。

## 二、数据定义（DDL）

### 2.1 基本表的操作

创建表时需要指定表名、列名、数据类型和完整性约束。

```sql
CREATE TABLE Student (
    Sno   CHAR(6) PRIMARY KEY,
    Sname CHAR(8) NOT NULL,
    Sage  INT,
    Ssex  ENUM('F', 'M'),
    Sdept CHAR(10),
    FOREIGN KEY (Sdept) REFERENCES Dept(Dno)
);
```

这段DDL语句创建了一个"学生表"，其中Sno是主码（实体完整性），Sname不能为空（用户定义完整性），Sdept是外码引用Dept表（参照完整性）。

修改表结构和删除表：
```sql
ALTER TABLE Student ADD COLUMN Email VARCHAR(50);
ALTER TABLE Student DROP COLUMN Email;
ALTER TABLE Student MODIFY COLUMN Sage SMALLINT;
DROP TABLE Student;
```

### 2.2 索引

索引是加快数据检索速度的重要手段。它可以建立在表的一列或多列上。

```sql
CREATE UNIQUE INDEX idx_sno ON Student(Sno);
CREATE INDEX idx_dept ON Student(Sdept);
DROP INDEX idx_sno ON Student;
```

UNIQUE索引要求被索引的列的值不能重复，这与主码约束类似。索引虽然能加速查询，但会降低更新操作的速度（因为每次更新都需要同步修改索引），所以索引并非越多越好。

## 三、数据查询（DQL）

查询是SQL中使用最频繁的操作，其基本结构为SELECT-FROM-WHERE。

### 3.1 单表查询

```sql
-- 查询所有列
SELECT * FROM Student;

-- 查询指定列
SELECT Sname, Sage FROM Student;

-- 带条件查询
SELECT Sname FROM Student WHERE Sdept = 'CS' AND Sage < 20;

-- 排序
SELECT Sno, Grade FROM SC WHERE Cno = '3' ORDER BY Grade DESC;

-- 用别名
SELECT Sname AS 姓名, Sage AS 年龄 FROM Student;

-- 消除重复
SELECT DISTINCT Sdept FROM Student;
```

### 3.2 聚集函数与分组

SQL提供了五个常用的聚集函数：COUNT（计数）、SUM（求和）、AVG（平均值）、MAX（最大值）、MIN（最小值）。它们通常与GROUP BY子句配合使用。

```sql
-- 统计学生总人数
SELECT COUNT(*) FROM Student;

-- 统计各课程的平均分
SELECT Cno, AVG(Grade) FROM SC GROUP BY Cno;

-- HAVING对分组结果进行筛选
SELECT Sno FROM SC
GROUP BY Sno
HAVING COUNT(*) > 2;
```

这里有一个容易混淆的地方：**WHERE子句用于分组前筛选行，HAVING子句用于分组后筛选组**。WHERE在GROUP BY之前执行，HAVING在GROUP BY之后执行。

### 3.3 连接查询

连接查询是SQL中最重要也是最常用的操作。当查询需要从多个表中获取数据时，就需要使用连接。

```sql
-- 内连接（等值连接）
SELECT Student.Sno, Sname, Cno, Grade
FROM Student, SC
WHERE Student.Sno = SC.Sno;

-- 复合条件连接
SELECT Student.Sno, Sname
FROM Student, SC
WHERE Student.Sno = SC.Sno
  AND SC.Cno = '2'
  AND SC.Grade > 90;
```

### 3.4 嵌套查询

嵌套查询是指一个SELECT语句中内嵌了另一个SELECT语句。内层查询的结果作为外层查询的条件。

```sql
-- IN子查询：找出与"刘晨"同系的学生
SELECT Sno, Sname, Sdept
FROM Student
WHERE Sdept IN (
    SELECT Sdept FROM Student WHERE Sname = '刘晨'
);

-- EXISTS子查询：找出选修了1号课程的学生
SELECT Sname FROM Student
WHERE EXISTS (
    SELECT * FROM SC
    WHERE SC.Sno = Student.Sno AND Cno = '1'
);
```

EXISTS和NOT EXISTS用于测试子查询的结果是否为空。它们通常比IN更灵活，可以实现"全称量词"的操作（如"找出选修了所有课程的学生"）。

## 四、数据更新（DML）

```sql
-- 插入
INSERT INTO Student VALUES ('95020', '陈冬', '男', 'IS', 18);

-- 更新（将所有学生年龄加1）
UPDATE Student SET Sage = Sage + 1;

-- 带子查询的更新
UPDATE SC SET Grade = 0
WHERE Sno IN (SELECT Sno FROM Student WHERE Sdept = 'CS');

-- 删除
DELETE FROM SC WHERE Cno = '2';
```

## 五、视图

视图是从一个或多个基本表中导出的虚表。视图不存储实际数据，只保存了定义。对视图的查询会在执行时实时转换为对基本表的查询。视图的主要作用是：简化查询（将复杂的查询定义为视图）、提供安全保护（只暴露需要的数据）和维护数据独立性。

```sql
CREATE VIEW IS_Student AS
SELECT Sno, Sname, Sage FROM Student WHERE Sdept = 'IS';

DROP VIEW IS_Student;
```

视图的可更新性有限制：包含聚集函数、DISTINCT、GROUP BY、多表连接的视图通常不能更新。

## 六、授权控制

```sql
GRANT SELECT ON TABLE Student TO U1;
GRANT ALL PRIVILEGES ON TABLE Student TO U2 WITH GRANT OPTION;
REVOKE SELECT ON TABLE Student FROM U1;
```

WITH GRANT OPTION允许用户将获得的权限再授予其他用户。
'''


def db_04():
    return '''# DB-04 数据库安全性与完整性

## 一、数据库安全性

### 1.1 安全性控制模型

数据库安全性控制贯穿数据访问的各个环节：

**事前控制**：用户标识和鉴别（输入用户名密码）、入侵检测
**事中控制**：存取控制（判断用户是否有权限访问数据）
**事后控制**：审计（记录用户的操作日志，用于事后追溯）
**存储保护**：敏感数据加密存储
**传输保护**：数据在网络传输过程中的加密

### 1.2 存取控制

数据库存取控制分为两种类型：

**自主存取控制（DAC）**：用户可以根据自己的意愿，将自己拥有的数据对象的访问权限授予其他用户。这是最常用的方式，通过SQL的GRANT和REVOKE语句实现。

**强制存取控制（MAC）**：系统根据主体和客体的安全级别进行控制。每个用户被分配一个"许可证级别"，每个数据对象被分配一个"密级"。只有当用户的安全级别大于等于数据的密级时，才能访问该数据。

### 1.3 课堂例题解析

**例题**：某数据库中有数据表Info，用户分为两类：管理员(admin)和访客(guest)。为每一位用户单独配置权限繁琐且易出错，请提出最优的权限管理方案。

> 解答：可以使用**数据库角色**机制。先创建admin和guest两个角色，为每个角色授予适当的权限，然后将用户分配到对应的角色中。这样当权限需要更新时，只需修改角色的权限，所有属于该角色的用户都会自动继承更新。
>
> ```sql
> CREATE ROLE admin;
> CREATE ROLE guest;
> GRANT ALL PRIVILEGES ON Info TO admin;
> GRANT SELECT ON Info TO guest;
> GRANT admin TO user_admin1, user_admin2;
> GRANT guest TO user_guest1, user_guest2;
> ```

## 二、数据库完整性

### 2.1 完整性约束定义

数据库完整性是指数据的正确性和相容性。例如："年龄必须是整数，且取值在14-29之间"是正确的，"性别必须是男或女"是相容的。

完整性约束的完整定义方式：

```sql
CREATE TABLE SC (
    SNO   CHAR(9),
    CNO   CHAR(4),
    GRADE SMALLINT,
    CONSTRAINT PK_SC PRIMARY KEY (SNO, CNO),
    CONSTRAINT FK_SNO FOREIGN KEY (SNO) REFERENCES Student(SNO),
    CONSTRAINT FK_CNO FOREIGN KEY (CNO) REFERENCES Course(CNO),
    CONSTRAINT CK_GRADE CHECK (GRADE >= 0 AND GRADE <= 100)
);
```

### 2.2 触发器

触发器是一种特殊的存储过程，它由DML事件（INSERT、UPDATE、DELETE）自动触发。触发器可以实现更复杂的完整性约束，如：

- 当更新学生年龄时，检查新年龄是否在合理范围内
- 当删除系别时，级联删除该系所有学生的选课记录
- 记录每次对重要数据的修改操作到审计日志

```sql
CREATE TRIGGER check_stu_age
BEFORE INSERT ON Student
FOR EACH ROW
BEGIN
    IF NEW.Sage < 14 OR NEW.Sage > 29 THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = '年龄必须在14-29之间';
    END IF;
END;
```
'''


def db_05():
    return '''# DB-05 关系数据理论

## 一、函数依赖

函数依赖是关系数据理论的核心概念。它描述了关系中属性之间的"决定"关系。

**定义**：设关系模式R(U,F)，U是属性全集。如果对于U的子集X和Y，对于R的任意一个实例，X的每个值都对应Y的唯一值，则称X函数决定Y，记作X→Y。

**一个直观的理解**：函数依赖类似于数学中的函数概念。如果我们知道一个人的学号X，就能唯一确定他的姓名Y，那么学号→姓名就是一个函数依赖。

### 函数依赖的分类

**平凡依赖**：如果Y是X的子集，那么X→Y是平凡的。例如(学号,姓名)→学号。平凡依赖总是成立的，没有实际意义。

**非平凡依赖**：如果Y不是X的子集，那么X→Y是非平凡的。例如学号→姓名。我们通常只关心非平凡的函数依赖。

**完全函数依赖**：如果X→Y，且X的任何真子集都不能决定Y，则称Y完全依赖于X。

**部分函数依赖**：如果X→Y，且X的某个真子集也能决定Y，则称Y部分依赖于X。

**传递函数依赖**：如果X→Y，Y→Z，且Y不能决定X（即Y→X不成立），则称Z传递依赖于X。

### 函数依赖的分类理解

假设有一个关系模式SCD(Sno, Cno, Grade, Age)，其中(Sno, Cno)是主码。

- Grade完全依赖于(Sno, Cno)——只知道学号或只知道课程号都无法确定成绩，必须两者同时知道才能确定
- Age部分依赖于(Sno, Cno)——因为Sno单独就能决定Age（学号决定年龄），所以Age部分依赖于主码
- 如果Sno→Dep（系别），Dep→Dean（系主任），则Dean传递依赖于Sno

## 二、范式及其判定

范式是衡量关系模式规范化程度的等级。从第一范式到第五范式，规范化程度越来越高。

### 2.1 第一范式（1NF）

要求：**关系中的每个属性都是不可再分的原子值**。

这是一张表成为"关系"的基本条件。违反的例子："电话号码"字段中存储了"手机：138xxxx，座机：0851-xxxx"这样的复合值。这样的设计会导致查询和更新变得非常困难。

### 2.2 第二范式（2NF）

要求：满足1NF，且**每个非主属性都完全函数依赖于主码**。

如果主码是联合主码（由多个属性构成），需要检查是否有非主属性只依赖于主码的一部分。如果有，就不满足2NF，需要进行模式分解。

**例题**：关系R(Sno, Cno, Grade, Age)，主码是(Sno, Cno)。其中Sno→Age（学号决定年龄），即Age部分依赖于主码。因此R不满足2NF。

**分解方案**：将R分解为R1(Sno, Cno, Grade)和R2(Sno, Age)。这样R1和R2都满足2NF。

### 2.3 第三范式（3NF）

要求：满足2NF，且**每个非主属性都不传递依赖于主码**。

如果有非主属性可以通过其他非主属性间接依赖于主码，就不满足3NF。

**例题**：关系R(Sno, Sname, Dept, Dean)，主码是Sno。假设Sno→Dept，Dept→Dean，那么Dean传递依赖于Sno。因此R不满足3NF。

**分解方案**：R1(Sno, Sname, Dept)和R2(Dept, Dean)。

### 2.4 BCNF

要求：满足1NF，且**所有函数依赖X→Y中，X必须包含候选码**。

BCNF比3NF更严格。一个关系满足3NF但不一定满足BCNF。

### 范式判定步骤

```
1. 求出所有候选码
2. 列出主属性和非主属性
3. 检查1NF（属性是否可再分）
4. 检查2NF（非主属性是否完全依赖于码）
5. 检查3NF（非主属性是否传递依赖于码）
6. 检查BCNF（所有决定因素是否都包含码）
```

## 三、闭包算法与候选码求解

### 3.1 属性集闭包

属性集闭包X+是从X出发，通过函数依赖能够推导出的所有属性的集合。

```
算法：
result = X
while (result发生变化) {
    for (F中的每个函数依赖Y→Z) {
        if (Y ⊆ result) result = result ∪ Z
    }
}
return result
```

**例题**：R(U,F)，U={A,B,C,D,E}，F={AB→C, B→D, C→E, EC→B, AC→B}，求(AB)+

> (AB)+
> 初始：AB
> AB→C → ABC
> B→D → ABCD
> C→E → ABCDE
> 结果 = ABCDE = U，所以AB是一个候选码

### 3.2 候选码求解

要找到关系模式的所有候选码，可以按照以下步骤：

**第一步**：将属性分为四类：
- L类：只在函数依赖左边出现的属性
- R类：只在右边出现的属性
- LR类：两边都出现的属性
- N类：两边都不出现的属性

**第二步**：候选码必包含所有L类和N类属性。

**第三步**：从L+N类属性的闭包开始，如果闭包不等于U，则逐步加入LR类属性进行扩展，直到闭包等于U。

**例题**：R(U,F)，U={A,B,C,D,E}，F={AB→C, B→D, C→E, EC→B, AC→B}

> L类：A
> R类：D
> LR类：B, C, E
> (A)+ = A ≠ U
> (AB)+ = ABCDE = U  ✓ (AB是候选码)
> (AC)+ = ACEBD = U  ✓ (AC也是候选码)
> 候选码：AB和AC
> 主属性：A, B, C
> 非主属性：D, E
> 因为B→D，D部分依赖于码AB，所以不满足2NF，最高为1NF

## 四、最小函数依赖集

最小函数依赖集Fm是一个与F等价但"更精简"的函数依赖集。它的三个条件：

1. **单属性化**：右部只有一个属性
2. **无冗余化**：没有多余的函数依赖
3. **左边既约化**：左边的属性不能化简

求解步骤：

```
例：F = {A→BC, E→C, D→AEF, ABF→BD}

① 右部分解：
   F = {A→B, A→C, E→C, D→A, D→E, D→F, ABF→B, ABF→D}

② 去掉冗余：
   ABF→B是平凡的（B⊆ABF），去掉
   ABF→D，检查(AF)+ = {ABFD}，所以AF→D，化简为AF→D

③ 结果：
   Fm = {A→B, A→C, E→C, D→A, D→E, D→F, AF→D}
```
'''


def db_06():
    return '''# DB-06 数据库设计

## 一、数据库设计的六个阶段

数据库设计是一个系统的工程，通常分为六个阶段：

**1. 需求分析阶段**：了解用户需要存储什么数据、支持什么操作。这是最困难、最耗费时间的一步。输出结果是需求说明书和数据字典。数据字典包括数据项、数据结构、数据流、数据存储和处理过程五部分。

**2. 概念结构设计阶段**：将需求分析的结果抽象为概念模型（通常用E-R图表示）。这个阶段独立于任何具体的DBMS，是最能体现设计水平的一步。

**3. 逻辑结构设计阶段**：将E-R图转换为DBMS支持的关系模式（即表结构）。在这个阶段开始需要考虑具体的DBMS特性。

**4. 物理设计阶段**：确定数据的存储结构和存取方法，如选择索引类型、决定文件的存放位置等。

**5. 数据库实施阶段**：使用DDL语句创建数据库和表，编写应用程序，加载初始数据。

**6. 数据库运行与维护阶段**：对数据库进行日常维护，包括性能优化、备份恢复、结构修改等。

## 二、E-R图向关系模式的转换规则

这是考试中极易出大题的部分，五条转换规则必须熟记：

**规则1**：一个实体型转换为一个关系模式。实体的属性成为关系的属性，实体的码成为关系的码。

**规则2**：一个1:n联系不转换为独立的关系模式，而是将"1端"的主码加入"n端"作为外码。

**规则3**：一个1:1联系的处理方式与1:n类似，可以选择任一端的主码加入另一端作为外码。

**规则4**：一个m:n联系必须转换为一个独立的关系模式。该关系模式的属性包括两端实体的码和联系本身的属性，码是两端码的组合。

**规则5**：三个或三个以上实体的多元联系也转换为一个独立的关系模式。

## 三、综合举例

**题目**：为仓库管理系统设计E-R图并转换为关系模式。

实体：仓库（仓库号，面积，电话）、零件（零件号，名称，规格，单价）、供应商（供应商号，姓名，地址，电话）、项目（项目号，预算，开工日期）、职工（职工号，姓名，年龄，职称）

联系：
1. 仓库和零件之间的"存放"联系，m:n，有属性"库存量"
2. 仓库和职工之间的"工作"联系，1:n
3. 职工之间的"领导"联系，1:n（自联系）
4. 供应商、项目、零件之间的"供应"联系，三元m:n联系，有属性"供应量"

**转换结果**：

```
仓库(仓库号, 面积, 电话) → PK: 仓库号
零件(零件号, 名称, 规格, 单价) → PK: 零件号
供应商(供应商号, 姓名, 地址, 电话) → PK: 供应商号
项目(项目号, 预算, 开工日期) → PK: 项目号
职工(职工号, 姓名, 年龄, 职称, 仓库号, 领导工号)
  → PK: 职工号, FK: 仓库号→仓库, FK: 领导工号→职工
存放(仓库号, 零件号, 库存量)
  → PK: (仓库号, 零件号), FK: 仓库号→仓库, FK: 零件号→零件
供应(供应商号, 项目号, 零件号, 供应量)
  → PK: (供应商号, 项目号, 零件号), FK: 供应商号→供应商, FK: 项目号→项目, FK: 零件号→零件
```

这个例子涵盖了E-R图转换的所有典型情况：普通实体、1:n联系、m:n联系、自联系和多元联系。
'''

print("=== Generating DB v2 notes ===")
notes = [
    ('DB-00-目录与总览.md', db_00()),
    ('DB-01-绪论.md', db_01()),
    ('DB-02-关系数据库.md', db_02()),
    ('DB-03-SQL语言.md', db_03()),
    ('DB-04-安全性与完整性.md', db_04()),
    ('DB-05-关系数据理论.md', db_05()),
    ('DB-06-数据库设计.md', db_06()),
]

for fn, content in notes:
    content = content.replace('__SVG_SCHEMA__', SVG_SCHEMA)
    w(fn, content.lstrip('\n'))

total = sum(len(re.findall(r'[一-鿿㐀-䶿]', c)) for _, c in notes)
print(f"DB total Chinese chars: {total}")
