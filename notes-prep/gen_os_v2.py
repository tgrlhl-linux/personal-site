#!/usr/bin/env python3
"""Generate detailed OS notes - v2 expanded version."""

import os

OUT = r'E:\Claudecode\personal-site\notes-prep'

def w(filename, content):
    path = os.path.join(OUT, filename)
    with open(path, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f'  OK {filename}')

def content_len(text):
    import re
    return len(re.findall(r'[一-鿿㐀-䶿]', text))

SVG_PROCESS = '''<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 600 280" width="100%" height="280">
  <ellipse cx="120" cy="140" rx="70" ry="35" fill="#e8f5e9" stroke="#43a047" stroke-width="2.5"/>
  <text x="120" y="140" text-anchor="middle" dominant-baseline="central" font-size="15" font-weight="bold" fill="#2e7d32">就绪</text>
  <ellipse cx="300" cy="60" rx="70" ry="35" fill="#e3f2fd" stroke="#1976d2" stroke-width="2.5"/>
  <text x="300" y="60" text-anchor="middle" dominant-baseline="central" font-size="15" font-weight="bold" fill="#1565c0">运行</text>
  <ellipse cx="300" cy="220" rx="70" ry="35" fill="#fff3e0" stroke="#e65100" stroke-width="2.5"/>
  <text x="300" y="220" text-anchor="middle" dominant-baseline="central" font-size="15" font-weight="bold" fill="#e65100">阻塞</text>
  <line x1="178" y1="118" x2="248" y2="78" stroke="#555" stroke-width="2" marker-end="url(#pa)"/>
  <text x="195" y="90" font-size="11" fill="#555">CPU调度</text>
  <line x1="260" y1="88" x2="188" y2="122" stroke="#555" stroke-width="2" marker-end="url(#pa)"/>
  <text x="195" y="110" font-size="11" fill="#555">时间片完/抢占</text>
  <line x1="310" y1="95" x2="310" y2="183" stroke="#555" stroke-width="2" marker-end="url(#pa)"/>
  <text x="325" y="140" font-size="11" fill="#555">等待I/O或资源</text>
  <line x1="240" y1="205" x2="188" y2="165" stroke="#555" stroke-width="2" marker-end="url(#pa)"/>
  <text x="170" y="200" font-size="11" fill="#555">I/O完成</text>
  <defs><marker id="pa" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto"><path d="M 0 0 L 10 5 L 0 10 z" fill="#555"/></marker></defs>
</svg>'''

SVG_BANKER = '''<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 560 400" width="100%" height="400">
  <rect x="20" y="10" rx="8" ry="8" width="520" height="40" fill="#e3f2fd" stroke="#1976d2" stroke-width="1.5"/>
  <text x="280" y="34" text-anchor="middle" font-size="13" fill="#1565c0">输入：Available, Max, Allocation, Need</text>
  <line x1="280" y1="50" x2="280" y2="70" stroke="#555" stroke-width="2" marker-end="url(#bb)"/>
  <rect x="20" y="70" rx="8" ry="8" width="520" height="40" fill="#fff9c4" stroke="#f9a825" stroke-width="1"/>
  <text x="280" y="94" text-anchor="middle" font-size="12" fill="#f57f17">Work=Available, 所有Finish[i]=false</text>
  <line x1="280" y1="110" x2="280" y2="130" stroke="#555" stroke-width="2" marker-end="url(#bb)"/>
  <polygon points="280,170 140,140 420,140" fill="#e8f5e9" stroke="#43a047" stroke-width="1.5"/>
  <text x="280" y="150" text-anchor="middle" font-size="12" fill="#2e7d32">存在Finish[i]=false且Need[i]<=Work？</text>
  <line x1="420" y1="155" x2="500" y2="155" stroke="#c62828" stroke-width="2"/>
  <line x1="500" y1="155" x2="500" y2="300" stroke="#c62828" stroke-width="2"/>
  <line x1="500" y1="300" x2="420" y2="300" stroke="#c62828" stroke-width="2"/>
  <text x="460" y="146" font-size="13" fill="#c62828">否</text>
  <line x1="280" y1="170" x2="280" y2="210" stroke="#2e7d32" stroke-width="2" marker-end="url(#bb)"/>
  <rect x="140" y="210" rx="8" ry="8" width="280" height="40" fill="#e8f5e9" stroke="#43a047" stroke-width="1.5"/>
  <text x="280" y="227" text-anchor="middle" font-size="12" fill="#2e7d32">Pi完成, Work+=Allocation[i]</text>
  <text x="280" y="242" text-anchor="middle" font-size="12" fill="#2e7d32">Finish[i]=true</text>
  <line x1="280" y1="250" x2="280" y2="270" stroke="#2e7d32" stroke-width="2" marker-end="url(#bb)"/>
  <polygon points="280,310 140,280 420,280" fill="#fce4ec" stroke="#c62828" stroke-width="1.5"/>
  <text x="280" y="295" text-anchor="middle" font-size="13" fill="#c62828">所有Finish[i]=true？</text>
  <line x1="280" y1="310" x2="280" y2="340" stroke="#555" stroke-width="2" marker-end="url(#bb)"/>
  <line x1="420" y1="295" x2="500" y2="295" stroke="#c62828" stroke-width="2"/>
  <text x="460" y="286" font-size="13" fill="#c62828">否</text>
  <rect x="40" y="350" rx="8" ry="8" width="220" height="40" fill="#c8e6c9" stroke="#2e7d32" stroke-width="1.5"/>
  <text x="150" y="374" text-anchor="middle" font-size="14" fill="#1b5e20">系统安全</text>
  <rect x="300" y="350" rx="8" ry="8" width="220" height="40" fill="#ffcdd2" stroke="#c62828" stroke-width="1.5"/>
  <text x="410" y="374" text-anchor="middle" font-size="14" fill="#b71c1c">系统不安全</text>
  <defs><marker id="bb" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto"><path d="M 0 0 L 10 5 L 0 10 z" fill="#555"/></marker></defs>
</svg>'''

SVG_REPLACE = '''<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 620 260" width="100%" height="260">
  <text x="10" y="18" font-size="12" font-weight="bold" fill="#333">页面引用串：7,0,1,2,0,3,0,4,2,3,0,3,2  内存3块</text>
  <text x="10" y="44" font-size="14" font-weight="bold" fill="#1565c0">FIFO</text>
  <rect x="10" y="50" rx="3" ry="3" width="145" height="20" fill="#e3f2fd" stroke="#90caf9" stroke-width="1"/>
  <text x="12" y="64" font-size="11" fill="#1565c0">7 0 1 2 3 0 4 2 3 0</text>
  <rect x="10" y="72" rx="3" ry="3" width="145" height="20" fill="#fff3e0" stroke="#ffcc80" stroke-width="1"/>
  <text x="12" y="86" font-size="11" fill="#e65100">缺页: 7,0,1,2,3,0,4 → 9次</text>
  <text x="175" y="44" font-size="14" font-weight="bold" fill="#2e7d32">LRU</text>
  <rect x="175" y="50" rx="3" ry="3" width="145" height="20" fill="#e8f5e9" stroke="#a5d6a7" stroke-width="1"/>
  <text x="177" y="64" font-size="11" fill="#2e7d32">7 0 1 2 3 0 4 2 3 0</text>
  <rect x="175" y="72" rx="3" ry="3" width="145" height="20" fill="#fff3e0" stroke="#ffcc80" stroke-width="1"/>
  <text x="177" y="86" font-size="11" fill="#e65100">缺页: 7,0,1,2,3,4 → 6次</text>
  <text x="340" y="44" font-size="14" font-weight="bold" fill="#e65100">OPT（最佳）</text>
  <rect x="340" y="50" rx="3" ry="3" width="145" height="20" fill="#fce4ec" stroke="#ef9a9a" stroke-width="1"/>
  <text x="342" y="64" font-size="11" fill="#c62828">7 0 1 2 3 4</text>
  <rect x="340" y="72" rx="3" ry="3" width="145" height="20" fill="#fff3e0" stroke="#ffcc80" stroke-width="1"/>
  <text x="342" y="86" font-size="11" fill="#e65100">缺页: 7,0,1,2,3,4 → 5次</text>
  <text x="10" y="130" font-size="14" font-weight="bold" fill="#333">影响缺页率的四个因素</text>
  <text x="20" y="150" font-size="12" fill="#555">1. 页面大小：页面越大，页表越小，但内部碎片和缺页传输开销增大</text>
  <text x="20" y="168" font-size="12" fill="#555">2. 进程分配的物理块数：块数越多，缺页概率越低</text>
  <text x="20" y="186" font-size="12" fill="#555">3. 置换算法：LRU和OPT优于FIFO，但OPT无法实现（需预知未来）</text>
  <text x="20" y="204" font-size="12" fill="#555">4. 程序局部性：时间局部性好的程序缺页率显著降低</text>
</svg>'''

def os_00():
    return '''# 《操作系统》复习笔记 · 目录

## 总览

本系列笔记覆盖操作系统课程全部核心章节，按照由浅入深的顺序组织。操作系统的学习需要建立"分层抽象"的思维模式，从底层的硬件管理到上层的用户接口，每一层都在为上层提供更便捷的服务。

## 章节速览

| 章节 | 核心主题 | 重点难点 |
|------|---------|---------|
| **OS-01 引论** | 操作系统类型、中断、CPU状态 | 中断与异常的区别、状态转换 |
| **OS-02 进程管理** | 进程映像、三态转换、通信、线程 | PCB作用、通信机制选择 |
| **OS-03 CPU调度** | 调度算法、甘特图、性能计算 | 各算法的优缺点和适用场景 |
| **OS-04 同步与死锁** | PV操作、信号量、银行家算法 | 生产者-消费者、死锁避免 |
| **OS-05 内存管理** | 分区分配、分页、分段、TLB | 地址变换过程 |
| **OS-06 虚拟内存** | 页面置换、工作集、颠簸 | FIFO/LRU/OPT置换过程 |
| **OS-07 I/O系统** | I/O控制方式、缓冲、SPOOLing | DMA原理、磁盘调度 |
| **OS-08 文件系统** | 目录结构、共享、保护 | 硬链接与软链接 |
| **OS-09 磁盘管理** | 外存分配、FAT、RAID | 混合索引访盘次数计算 |

## 复习策略

1. 操作系统涉及大量概念对比（如分页vs分段、进程vs线程），建议采用"对比记忆法"
2. 计算类题目（调度算法、页面置换、银行家算法）需要动手画图，只看不练容易算错
3. 近三年真题中，PV操作与银行家算法交替出现在大题中，建议两手准备
4. 概念题主要集中在前三章和后两章，中段章节偏计算

## 符号约定

- KB = 千字节（2^10 字节）
- PCB = 进程控制块（Process Control Block）
- TLB = 快表（Translation Lookaside Buffer）
- SPOOLing = 假脱机操作（Simultaneous Peripheral Operation On-Line）
'''


def os_01():
    return '''# OS-01 操作系统引论

## 一、操作系统的基本概念

操作系统是计算机系统中最基本的系统软件，它管理计算机的硬件资源，为应用程序提供运行环境，同时也是用户与计算机硬件之间的接口。操作系统的核心目标是方便用户使用和提高资源利用率。要理解操作系统，首先需要了解它经历了怎样的发展历程，以及不同类型操作系统的各自特点。

### 1.1 操作系统的发展与类型

操作系统的发展经历了从简单到复杂、从低效到高效的过程。最早期的计算机没有操作系统，用户直接使用机器语言编程，需要手动操作硬件。随着计算机的发展，操作系统的形态也在不断演进。

**单道批处理系统**是操作系统的雏形。它的工作方式是内存中只存放一道用户作业，CPU依次执行，直到当前作业完成才载入下一个。这种方式的优点是实现简单，但缺点也是致命的：当CPU在执行I/O操作时，只能等待，无法处理其他任务，导致CPU利用率极低。例如，一个需要频繁读磁盘的作业在执行时，CPU大部分时间都在空转等待。

**多道批处理系统**的出现是一个重大突破。它允许在内存中同时存放多道程序，当一个程序等待I/O时，CPU立即切换到另一个程序执行。这种"交替执行"的方式充分利用了CPU的计算能力。举例来说，假设三个作业A、B、C分别需要计算资源10秒和I/O操作5秒，单道批处理的总耗时是45秒（三个作业串行），而多道批处理可以通过重叠计算与I/O将总耗时压缩到大约25秒。但多道批处理的缺点在于作业的周转时间变长了，而且系统管理复杂度显著提高。

**分时系统**的出现解决了多道批处理缺乏交互性的问题。它通过时间片轮转的方式，让多个用户通过终端同时与计算机交互。每个用户获得一个短暂的时间片轮流使用CPU，由于时间片极短（通常为几十毫秒），每个用户都感觉自己在独占计算机。分时系统的核心指标是响应时间——用户发出指令到得到反馈的时间间隔。

**实时系统**则专注于满足严格的时间约束。它要求系统必须在规定的时间内对外部事件做出响应，这个截止时间是硬性的，错过了就可能导致灾难性后果。实时系统分为硬实时（如航空控制系统，必须严格遵守时间约束）和软实时（如视频播放，偶尔的超时不会造成灾难性后果）。

### 1.2 多道程序设计的核心概念

多道程序设计是现代操作系统的基础思想。它的本质是在内存中同时驻留多个程序，使它们交替执行，实现CPU与I/O设备的高度并行工作。

多道程序设计的优点非常突出：首先，它极大地提高了CPU的利用率——当一个程序在等待I/O时，CPU并不空闲，而是去执行另一个程序。其次，系统的吞吐量（单位时间内完成的作业数量）也得到了显著提升。然而，缺点同样不可忽视：单个作业的周转时间比单道系统更长，因为需要与其他作业共享CPU，系统的管理和调度也变得更加复杂。

理解**并行和并发的区别**是学习操作系统的第一道门槛。并行（Parallelism）是指多个事件在同一时刻真正同时发生，这需要系统具备多个处理器或处理器核心。并发（Concurrency）则是指多个事件在同一个时间间隔内交替发生，从宏观上看像是同时进行的，但实际上在单核处理器中，任何时刻都只有一个程序在执行。打个比方：并行就像两条并行的传送带同时在生产产品，而并发就像一个人在处理多个任务，通过快速地任务切换让人感觉所有任务都在"同时"推进。

**一道常见的面试题**：两个进程共享一个全局变量count（初始为0），每个进程都对count执行100次count++操作。在并发环境下，count最终的可能取值范围是什么？

> count++在机器层面需要三条指令：LOAD count到寄存器、在寄存器中加1、STORE count回内存。当两个进程并发执行时，这三条指令可能以任意顺序交错。最好的情况是每次交错都完整执行，最终得到200。最差的情况是两个进程同时LOAD到相同的值，各自加1后写回，导致一次增加被"覆盖"。理论上极端情况下每次都被覆盖，最终结果为2。所以可能范围是 [2, 200]。

## 二、中断与异常

中断机制是现代操作系统的"心脏"。没有中断，操作系统无法响应外部事件，也无法实现多道程序设计的"交替执行"。

### 2.1 中断与异常的分类和区别

中断（Interrupt）和异常（Exception）虽然都会导致CPU暂停当前程序转去执行特定处理程序，但它们的根本来源不同。

**中断**是由CPU外部的事件引起的。典型的例子包括：磁盘I/O完成、键盘被按下、鼠标移动、网卡收到数据包、时钟芯片产生的定时中断等。这些事件的共同特点是它们来自CPU外部，是不可预测的异步事件。

**异常**则是由CPU内部的事件引起的，通常是正在执行的指令本身出现了问题。例如：程序执行了除零操作、访问了非法的内存地址、执行了不在指令集中的无效操作码、调用了系统调用指令。异常是与当前指令同步的，也就是说，如果重新执行同样的指令，同样的异常会再次发生。

判断一个事件是否中断还是异常的方法是看它的来源：来自外部设备的是中断，来自CPU内部的是异常。

### 2.2 中断处理过程

当中断发生时，CPU需要暂停当前程序的执行，转去处理中断事件，处理完成后再回到原来的程序继续执行。这个过程的完整步骤是：

**第一步，关中断**。在保存现场的过程中，CPU不能再响应其他中断，否则可能导致保存的现场数据不一致。

**第二步，保存现场**。将当前程序的程序计数器（PC）、程序状态字（PSW）、寄存器等关键信息保存到栈中，以便后续恢复。

**第三步，识别中断源**。CPU需要判断是哪个设备发出了中断，这个过程通常通过查询中断向量表来完成。

**第四步，执行中断处理程序**。根据识别出的中断类型跳转到对应的处理函数执行。

**第五步，恢复现场**。中断处理完成后，将之前保存的现场数据恢复到寄存器中。

**第六步，开中断并返回**。重新开启中断响应能力，通过iret指令（x86架构）返回到被中断的程序继续执行。

## 三、CPU的两种运行状态

为了保护操作系统不被用户程序破坏，CPU设计了两种运行状态：内核态（又称管态、系统态）和用户态（又称目态）。

**内核态**是操作系统运行时的状态。在内核态下，CPU可以执行所有指令，包括特权指令。特权指令是那些可能破坏系统稳定性的指令，如修改内存管理单元（MMU）、设置时钟、清除内存、修改PSW等。操作系统内核负责管理这些关键资源，所以它必须运行在内核态。

**用户态**是普通用户程序运行时的状态。在用户态下，CPU只能执行非特权指令，不能执行前述的特权指令。如果用户程序在用户态下试图执行特权指令，CPU会产生一个异常（称为"保护异常"），由操作系统接管处理。

**状态转换的机制**：

从用户态切换到内核态只有三种途径：系统调用、中断和异常。这三种方式的共同特点是用户程序主动或被动地"陷入"内核。转换是通过硬件机制完成的，用户程序无法直接修改CPU的状态寄存器来进入内核态，这保证了操作系统的安全性。

从内核态切换回用户态只有一种方式：由操作系统执行特权指令来修改PSW中的状态位，实现从内核态到用户态的转换。这个转换是操作系统主动进行的，发生在系统调用或中断处理完成后。

**一个直观的例子**：当你在应用程序中调用read()函数读取文件时（用户态），read()会触发系统调用指令，CPU切换到内核态，由文件系统驱动在内核态下执行实际的磁盘读取操作，读取完成后操作系统修改PSW，CPU返回用户态，应用程序继续执行。

## 四、系统调用

系统调用是操作系统提供给用户程序的服务接口。当用户程序需要执行需要特权的操作时（如读写文件、创建进程、分配内存），它无法直接在用户态完成，而是通过系统调用请求操作系统代为执行。

**系统调用的处理过程**：
1. 用户程序调用库函数（如C语言中的printf），库函数将参数整理好
2. 库函数执行一条访管指令（trap指令），触发CPU从用户态切换到内核态
3. 操作系统根据系统调用号找到对应的内核处理函数
4. 内核函数在内核态下执行所需操作
5. 操作完成后，CPU返回用户态，结果返回给用户程序

**需要系统调用的常见操作**：文件操作（打开、读写、关闭）、进程管理（创建、终止）、内存管理（申请、释放）、设备操作（读键盘、写显示器）、网络通信等。

## 五、操作系统结构

操作系统本身也是一个软件，它的内部组织结构直接影响到系统的可靠性、可维护性和性能。

| 结构类型 | 特点 | 典型系统 | 评价 |
|---------|------|---------|------|
| **简单结构** | 没有清晰分层，模块间可以互相调用 | MS-DOS、早期UNIX | 效率高但维护困难，一处修改可能影响全局 |
| **分层结构** | 分为若干层，每层只依赖下层提供的服务 | THE系统 | 结构清晰，便于调试，但层间通信开销大 |
| **微内核** | 内核只保留最基本的功能（进程通信、最小调度） | Minix、QNX | 可靠性高，扩展性好，但性能开销较大 |

现代主流操作系统（Linux、Windows）采用混合结构，将性能关键的模块放在内核中运行，同时通过模块化机制保持了一定的灵活性和可维护性。

## 六、例题精讲

**判断题**：以下事件分别属于中断还是异常？
1. 硬盘DMA传输完成 → 中断（外部设备事件）
2. 程序发生段错误 → 异常（CPU内部检测到的非法访问）
3. 定时器到期 → 中断（外部时钟硬件）
4. 整数除零 → 异常（算术运算异常）
5. 用户按下Ctrl+C → 中断（键盘输入事件）
6. 缺页 → 异常（CPU访问内存时发现页面不在内存）

**简答题**：系统调用与普通函数调用的区别是什么？

> 系统调用需要CPU状态切换（用户态→内核态→用户态），而普通函数调用在用户态内完成。系统调用有较大的开销（数百个CPU周期），因为它涉及状态保存、特权级切换、返回等操作。普通函数调用仅涉及栈操作，开销很小（数个CPU周期）。
'''


def os_02():
    return f'''# OS-02 进程描述与控制

## 一、进程的基本概念

进程是操作系统中最重要的概念之一。简单来说，进程就是正在执行的程序的一个实例。但"正在执行"包含了比程序本身更多的内涵——程序是静态的、存储在磁盘上的指令序列，而进程是动态的，包括了程序代码、当前的活动状态以及相关的资源。

### 1.1 进程映像的三要素

一个进程在内存中的组织形式称为**进程映像**（Process Image），它由三个核心部分组成：

**程序段（代码段）**：存放可执行程序的指令代码。这是程序本身的二进制表示，多个进程可以共享同一段代码（例如多个用户同时运行同一个编辑器，但每个进程有自己的数据段）。

**数据段**：存放程序运行过程中使用的全局变量、静态变量等数据。这里的"数据"是程序需要操作的对象。

**进程控制块（PCB）**：这是进程存在的唯一标识，也是操作系统管理进程的核心数据结构。PCB中记录了进程的各种信息，主要包括：
- 进程标识符（PID），用于唯一标识一个进程
- 进程状态（就绪、运行、阻塞等）
- CPU寄存器的值（当进程被切换时需要保存）
- 程序计数器（下一条要执行的指令地址）
- 内存分配信息（页表、段表等）
- 打开的文件列表
- 优先级、调度信息
- 进程间通信相关的信息

PCB常驻在内存的系统空间中。操作系统通过PCB来感知和管理进程——当一个进程创建时，内核为其分配一个PCB；当进程结束时，内核回收这个PCB。

**一个重要的特性**：进程是资源拥有的基本单位。操作系统以进程为单位分配内存、文件句柄等资源。这与后面要讲的"线程"形成了对比——线程是CPU调度的基本单位，但不拥有资源。

## 二、进程的状态与转换

进程在执行过程中会经历不同的状态。操作系统根据这些状态来决定"接下来该运行哪个进程"。

### 2.1 三态模型

{SVG_PROCESS}

**就绪态**：进程已经获得了除CPU之外的所有资源，只等待CPU调度器的分配。在就绪队列中，可能有多个进程在等待CPU，调度器需要从中选择一个来执行。

**运行态**：进程获得了CPU的使用权，正在执行指令。在单核CPU中，任何时候只有一个进程处于运行态。

**阻塞态**：进程因为等待某个事件的发生（如I/O操作完成、获取锁、接收到消息）而暂停执行。进入阻塞态的进程会主动放弃CPU，调度器会从就绪队列中选择另一个进程运行。当等待的事件发生时，阻塞态的进程会被唤醒，转移到就绪态。

**四种转换关系**：

就绪→运行：调度器选中该进程，分配CPU给它。这是调度器的核心职责。

运行→就绪：有两种情况——一种是该进程的时间片用完了，被调度器强制切换出去；另一种是被更高优先级的进程抢占（在可抢占调度算法中）。

运行→阻塞：进程主动请求等待某种资源或事件。这是进程自身的主动行为，比如请求一个暂时不可用的锁、等待I/O完成等。

阻塞→就绪：进程等待的事件发生了。这是由外部事件驱动的，例如I/O完成中断服务程序会唤醒等待I/O完成的进程。

**一道典型题目**：一个进程在运行过程中，以下哪些事件会导致它从运行态变为阻塞态？
A. 时间片用完  B. 请求读磁盘文件  C. 被更高优先级进程抢占  D. 收到其他进程发送的消息

> 答案：B和D。A和C是运行→就绪（被动地被调度器切换）。B和D是进程主动等待事件（读磁盘需等待I/O完成、接收消息需等待消息到达）。

## 三、进程通信

进程之间相互独立运行，但很多时候它们需要协同工作，这就需要在进程之间传递信息。操作系统提供了三种基本的进程通信（IPC）机制。

### 3.1 共享内存

共享内存是最快的一种IPC方式。它的工作原理是：操作系统将同一块物理内存区域映射到多个进程的地址空间中，这些进程就可以直接读写这块内存来实现数据共享。由于数据不需要在进程间复制，共享内存的通信速度非常快。

但共享内存也带来了同步问题——多个进程同时读写共享区域可能导致数据不一致。因此，共享内存通常需要与其他同步机制（如信号量）配合使用。

### 3.2 消息传递

消息传递机制通过操作系统提供的send和receive系统调用来实现进程间通信。进程A将消息通过操作系统发送给进程B，操作系统负责消息的缓冲和路由。消息传递分为直接通信（直接指定接收进程）和间接通信（通过信箱/端口）两种方式。

消息传递的优势在于不需要共享内存，所以适合分布式系统中的进程通信。缺点是消息的发送和接收涉及系统调用，性能不如共享内存。

### 3.3 管道

管道是一个连接读写进程的字节流，本质上是一个内核缓冲区。数据从管道的一端写入，从另一端读出。管道是半双工的（Unix/Linux管道的原始设计），数据只能向一个方向流动。管道的缓冲区有固定的容量限制（在Linux中通常为65536字节）。

命名管道（FIFO）是管道的扩展，它允许两个无亲缘关系的进程通过一个在文件系统中命名的管道来通信。

## 四、进程与线程

### 4.1 线程的引入

进程有两个基本属性：资源拥有的基本单位和CPU调度的基本单位。但这两个属性产生了矛盾——进程的创建、切换和销毁都需要较大的开销（因为要处理资源的分配和回收）。为了减少这些开销，线程的概念被引入。

**线程**（Thread）被设计为CPU调度的基本单位，而进程仍作为资源拥有的基本单位。多个线程可以存在于同一个进程中，共享进程的资源（代码段、数据段、打开的文件等），但每个线程拥有自己独立的栈空间、寄存器和程序计数器。

### 4.2 进程与线程的区别

- **资源拥有**：进程是资源拥有的基本单位，线程不拥有资源，共享所属进程的资源
- **调度单位**：线程是CPU调度的基本单位
- **切换开销**：进程切换需要切换地址空间，开销大；线程切换不涉及地址空间切换，开销小
- **通信方式**：进程间通信需要IPC机制；线程间可以直接读写共享内存（但需同步）
- **独立性**：进程独立性强，一个进程崩溃不影响其他进程；线程崩溃可能影响整个进程

**共享资源**：代码段、数据段、打开的文件、信号处理函数、工作目录

**非共享资源**：程序计数器、寄存器、栈空间、线程ID

### 4.3 用户级线程与内核级线程

**用户级线程**的管理完全在用户空间完成，内核不知道线程的存在。线程调度由用户空间的线程库（如POSIX Threads库的许多实现）负责，切换时不需要系统调用，速度非常快。但缺点是如果某一个线程执行了阻塞的系统调用，整个进程的所有线程都会阻塞。

**内核级线程**由操作系统内核直接管理。线程的创建、调度、销毁都由内核完成。内核知道每个线程的存在，可以利用多处理器来并行执行多个线程。当一个线程阻塞时，内核可以调度同一进程中的另一个线程。缺点是线程切换需要系统调用，开销较大。

**一个典型例子**：假设你打开一个文本编辑器，这个程序以一个进程运行。你在这个编辑器中打开了三个文件进行编辑——每个文件的编辑可能由一个线程负责。这三个线程共享同一份程序代码和已打开文件的列表，但每个线程可以独立地滚动浏览自己的文件内容。
'''


def os_03():
    return '''# OS-03 处理机调度

## 一、调度算法概述

CPU调度是操作系统最核心的功能之一。当多个进程同时处于就绪态时，调度器需要决定"谁先用CPU"。不同的调度算法对系统性能的影响是巨大的。我们先来理解几个衡量调度算法性能的关键指标。

## 二、关键性能指标

**周转时间（Turnaround Time）**：从进程到达系统到它完成任务所经过的时间。即：完成时间 − 到达时间。这个指标反映了一个进程从提交到完成的总耗时。

**等待时间（Waiting Time）**：进程在就绪队列中等待CPU的总时间。注意：等待时间不包括正在执行的时间。即：周转时间 − 执行时间。

**响应时间（Response Time）**：从进程提交到首次获得CPU的时间。在交互式系统中，响应时间比周转时间更重要。

**带权周转时间**：周转时间 / 执行时间。这个值反映了执行的"效率"，最小为1（没有任何等待，一到就执行）。

## 三、常见调度算法详解

### 3.1 先来先服务（FCFS）

FCFS是最简单的调度算法——按照进程到达就绪队列的顺序依次调度。就像顾客在银行柜台排队一样，先到的人先办理业务。

**优点**：实现简单，公平。每个进程迟早都能被执行。

**缺点**：短作业可能需要等待很长的时间（排在长作业后面时）。假设一个需要执行1秒的作业到达时，正好在前面有一个需要执行100秒的作业，那么短作业的等待时间就是100秒。

### 3.2 短作业优先（SJF）

SJF选择执行时间最短的就绪进程来执行。这是一种"贪心"策略，目标是最小化平均等待时间。

**优点**：理论上可以证明SJF能给出最小的平均等待时间。对于批处理系统来说，这是最优的调度策略。

**缺点**：需要预知进程的执行时间，这在实践中几乎不可能实现。长作业可能永远得不到执行（饥饿现象），因为系统中只要有短作业到来，长作业就轮不到。

### 3.3 响应比高者优先（HRRN）

HRRN是对SJF的改进，它引入了"响应比"的概念：响应比 = (等待时间 + 执行时间) / 执行时间。

这个公式的含义是：执行时间短的作业有更高的响应比（效率优先），而等待时间长的作业的响应比会随着等待时间的增加而增加（避免饥饿）。这样既兼顾了效率又保证了公平。

### 3.4 时间片轮转（RR）

RR是分时系统的核心算法。每个进程被分配一个固定的时间片（例如100ms），用一个接一个地轮流执行。如果一个进程在时间片内没有完成，它被放回就绪队列的尾部，调度器选择下一个进程执行。

**时间片的选择至关重要**：
- 时间片太大 → 退化为FCFS，响应时间变长
- 时间片太小 → 频繁的上下文切换，CPU的有效利用率下降（大量时间花在切换而非计算上）

### 3.5 多级反馈队列

这是现代操作系统中最常用的调度算法。它设置了多个不同优先级的队列，每个队列有不同的时间片大小。新到达的进程先进入最高优先级队列（时间片最小）。如果进程在时间片内没有完成，被降级到下一个优先级队列（时间片更大）。这种设计兼顾了I/O密集型和CPU密集型进程的不同需求。

## 四、典型例题精讲

设有四个进程：

| 进程 | 到达时间 | 执行时间 |
|------|---------|---------|
| P1 | 0 | 8 |
| P2 | 1 | 4 |
| P3 | 2 | 9 |
| P4 | 3 | 5 |

### 4.1 先来先服务（FCFS）

执行顺序：P1(0-8) → P2(8-12) → P3(12-21) → P4(21-26)

计算结果：
```
P1: 完成时间=8, 周转时间=8,  带权=1.0, 等待时间=0
P2: 完成时间=12, 周转时间=11, 带权=2.75, 等待时间=7
P3: 完成时间=21, 周转时间=19, 带权=2.11, 等待时间=10
P4: 完成时间=26, 周转时间=23, 带权=4.6,  等待时间=18

平均周转时间 = (8+11+19+23)/4 = 15.25
平均带权周转时间 = (1+2.75+2.11+4.6)/4 = 2.615
```

FCFS的缺点是显而易见的：P4虽然只执行5秒，但要等到P3执行完才能运行，等待了18秒。

### 4.2 非抢占式短作业优先（SJF）

执行顺序：P1(0-8) → P2(8-12) → P4(12-17) → P3(17-26)

在t=8时，P2(4)、P3(9)、P4(5)都已到达，选最短的P2执行
在t=12时，P4(5) < P3(9)，选P4
最后执行P3

```
平均周转时间 = (8+11+14+24)/4 = 14.25
```

相比FCFS，SJF的平均周转时间更短。

### 4.3 时间片轮转法（RR, q=2）

执行序列（按时间点展开）：
t=0: P1开始 → t=2: P1时间片完，P2开始
t=4: P2时间片完，P3开始
t=6: P3时间片完，P4开始
t=8: P4时间片完，P1继续 → t=10: P1时间片完，P2继续
t=11: P2执行完毕，P3继续 → t=13: P3时间片完，P4继续
t=15: P4时间片完，P1继续 → t=17: P1时间片完，P3继续
t=19: P3时间片完，P4继续 → t=20: P4执行完毕
t=22: P1继续 → t=24: P1执行完毕，P3继续
t=26: P3执行完毕

```
平均周转时间 = (24+10+24+19)/4 = 19.25
```

RR的周转时间比FCFS和SJF都长，但响应时间最短（所有进程都在2个单位时间内获得首次响应）。
'''


def os_04():
    return '''# OS-04 进程同步与死锁

## 一、同步与互斥的基本概念

当多个进程并发执行时，它们之间可能产生相互影响。这种影响分为两类：同步和互斥。

**同步（Synchronization）**：多个进程在执行顺序上需要遵循一定的约束关系。例如，如果进程B需要使用进程A产生的数据，那么进程A必须在进程B之前完成数据的生产。这种"先后顺序"的约束就是同步。经典的例子是生产者-消费者问题——消费者必须在生产者生产了商品之后才能消费。

**互斥（Mutual Exclusion）**：多个进程不能同时使用某个共享资源（称为"临界资源"）。例如，打印机在任何时刻只能为一个进程服务，如果两个进程同时向打印机输出，打印出来的内容会交错在一起。需要互斥访问的资源就是临界资源。

### 临界区与临界资源的访问规则

访问临界资源的代码段称为"临界区"。对临界区的访问必须遵循四条规则：
1. 空闲让进：当没有进程在临界区时，允许一个进程进入
2. 忙则等待：当已有进程在临界区时，其他进程必须等待
3. 有限等待：等待进入临界区的进程不会无限期等待
4. 让权等待：当进程不能进入临界区时，应释放CPU（切换到阻塞态）

## 二、信号量与PV操作

信号量是Dijkstra提出的一种解决同步与互斥问题的经典机制。信号量本质上是一个整型变量，它代表某种资源的可用数量。操作系统提供了两个原子操作（不可中断的操作）来操作信号量。

**信号量的含义**：
- S ≥ 0：表示当前可用的资源数
- S < 0：|S| 表示正在等待该资源的进程数

**P操作（wait操作，荷兰语"proberen"，意为"尝试"）**：
```
S = S - 1
如果 S < 0，则进程进入阻塞状态（等待队列）
```

**V操作（signal操作，荷兰语"verhogen"，意为"增加"）**：
```
S = S + 1
如果 S ≤ 0，则从等待队列中唤醒一个进程
```

### 用信号量实现互斥

```c
semaphore mutex = 1;  // 互斥信号量，初始为1

// 进程A                          // 进程B
P(mutex);                         P(mutex);
// 临界区操作                      // 临界区操作
V(mutex);                         V(mutex);
```

当第一个进程执行P(mutex)时，mutex变为0，进程进入临界区。第二个进程再执行P(mutex)时，mutex变为-1，进程阻塞。当第一个进程执行V(mutex)后，mutex变为0，唤醒第二个进程。这样就实现了"同一时间只有一个进程进入临界区"。

### 经典问题：生产者-消费者问题

生产者-消费者是最经典的多进程同步问题。生产者和消费者共享一个容量为n的缓冲区，生产者向缓冲区放入数据，消费者从缓冲区取出数据。需要保证：
1. 缓冲区满时，生产者不能继续放入（同步约束）
2. 缓冲区空时，消费者不能取出（同步约束）
3. 生产者和消费者不能同时操作缓冲区（互斥约束）

```c
semaphore mutex = 1;   // 互斥访问缓冲区
semaphore empty = n;   // 空缓冲区数量，初始为n
semaphore full = 0;    // 满缓冲区数量，初始为0

producer() {
  while (true) {
    produce_item();      // 生产数据
    P(empty);            // 申请一个空位
    P(mutex);            // 锁定缓冲区
    put_item();          // 放入数据
    V(mutex);            // 解锁缓冲区
    V(full);             // 增加一个满位
  }
}

consumer() {
  while (true) {
    P(full);             // 申请一个满位
    P(mutex);            // 锁定缓冲区
    get_item();          // 取出数据
    V(mutex);            // 解锁缓冲区
    V(empty);            // 增加一个空位
    consume_item();      // 消费数据
  }
}
```

**注意**：P(empty)必须在P(mutex)之前执行。如果颠倒了顺序，缓冲区满时会发生死锁——生产者持有mutex后等待empty，而消费者因为无法获取mutex而无法消费。这就是"死锁"的一个活生生例子。

## 三、死锁

### 3.1 死锁的四个必要条件

死锁是指多个进程因竞争资源而陷入相互等待的状态——每个进程都在等待对方释放资源，但谁都无法推进。死锁的产生必须同时满足四个条件：

1. **互斥条件**：至少有一个资源只能被一个进程同时使用（即资源是不可共享的）
2. **请求与保持条件**：进程已经获得了一些资源，同时在请求其他资源（在等待时不释放已获得的资源）
3. **不剥夺条件**：进程已获得的资源在未使用完之前不能被强行剥夺
4. **循环等待条件**：存在一个进程—资源的循环等待链，链中的每个进程都在等待下一个进程释放资源

这四个条件缺一不可，破坏任何一个条件就可以预防死锁。

### 3.2 死锁的预防策略

- **破坏互斥条件**：如果资源可以共享，就不会有死锁。但很多资源（如打印机）天生就是独占的，无法共享。这个策略的实用性不强。

- **破坏请求与保持条件**：要求进程在运行前一次性申请所有需要的资源。如果有一个资源申请不到，就释放所有已获得的资源。这种策略的缺点是资源利用率低，因为进程可能在很长时间内占用着暂时用不到的资源。

- **破坏不剥夺条件**：当一个进程申请的资源被其他进程占有时，系统可以强行剥夺已分配给其他进程的资源。这种策略实现复杂，且可能导致进程状态丢失。

- **破坏循环等待条件**：给所有资源编号，要求进程必须按编号升序申请资源。这样就不会形成循环等待。例如，打印机编号1，磁盘编号2，CD-ROM编号3，进程必须按1→2→3的顺序申请资源。

### 3.3 银行家算法

银行家算法是Dijkstra提出的死锁避免算法。它类似于银行贷款的审批过程——银行不会把所有资金都贷出去，而是保留一部分以应对突发的还款需求。

__SVG_BANKER__

算法的核心思想是：在系统分配资源之前，先判断分配后系统是否处于安全状态。如果是，才进行分配；否则，让进程等待。

**典型例题**：

系统有A、B、C三类资源共10、5、7个单位。五个进程的分配情况如下：

| 进程 | Allocation(A,B,C) | Max(A,B,C) | Need(A,B,C) |
|------|------|------|------|------|------|------|
| P0 | 0,1,0 | 7,5,3 | 7,4,3 |
| P1 | 2,0,0 | 3,2,2 | 1,2,2 |
| P2 | 3,0,2 | 9,0,2 | 6,0,0 |
| P3 | 2,1,1 | 2,2,2 | 0,1,1 |
| P4 | 0,0,2 | 4,3,3 | 4,3,1 |

Available(A,B,C) = (3,3,2)

验证系统是否安全：
1. 寻找Need ≤ Available的进程。P1的Need=(1,2,2) ≤ (3,3,2)，假设P1获得资源并完成，释放(2,0,0)，Available=(5,3,2)
2. P3的Need=(0,1,1) ≤ (5,3,2)，P3完成，释放(2,1,1)，Available=(7,4,3)
3. P4的Need=(4,3,1) ≤ (7,4,3)，P4完成，释放(0,0,2)，Available=(7,4,5)
4. P0的Need=(7,4,3) ≤ (7,4,5)，P0完成，释放(0,1,0)，Available=(7,5,5)
5. P2的Need=(6,0,0) ≤ (7,5,5)，P2完成

所有进程均可完成 → 系统处于安全状态，安全序列为 P1→P3→P4→P0→P2

### 3.4 死锁的资源计算

假设系统中有n个进程，每个进程最多需要m个某类资源，问系统至少需要多少个该类资源才能保证不会发生死锁？

**公式**：R ≥ n×(m-1) + 1

**推理过程**：最坏情况下，每个进程已经分到了(m-1)个资源，共n×(m-1)个资源。此时，只要还有一个空闲资源，任何进程都可以获得第m个资源后完成，释放资源后其他进程也能继续。如果资源数等于n×(m-1)，最坏情况是所有进程都差一个资源，谁都完成不了 → 死锁。

**例题**：系统有4个进程和5台磁带机，每个进程最多需要2台，问是否会死锁？
> R = 5, n×(m-1)+1 = 4×(2-1)+1 = 5
> 5 ≥ 5，所以不会死锁。但如果只有4台，则4 < 5，存在死锁风险。
'''

def os_05():
    return '''# OS-05 存储器管理

## 一、程序的装入与链接

一个程序从源代码到在内存中执行，需要经历编译、链接和装入三个阶段。其中链接和装入方式会直接影响程序的执行效率和内存占用。

**静态链接**：在程序运行之前，将所有需要的目标模块和库函数链接成一个完整的可执行文件。优点是运行时不需额外加载，缺点是浪费空间（多个程序可能包含相同的库函数副本）且更新困难。

**装入时动态链接**：在将程序装入内存时，边装入边链接。用到的模块才被装入内存，未用到的模块不装入。这种方式灵活性更高。

**运行时动态链接**：程序在运行过程中，需要某个模块时才进行链接和装入。这是最灵活的方式，常用于插件系统和共享库。Linux中的共享库（.so文件）就是采用这种方式的典型例子。

## 二、可变分区分配算法

在可变分区分配中，内存被动态地划分为大小不同的分区。当需要为一个进程分配内存时，系统从空闲分区列表中选择一个合适的区域。

**首次适应算法（FF）**：从内存低地址开始，选择第一个大小足够容纳进程的空闲分区。这是最简单的算法，实现速度快，但容易在低地址区域产生大量碎片。

**最优适应算法（OF）**：从所有空闲分区中选择大小最接近进程需求的分区。这样可以减少大分区被分割的可能性，但会产生很多难以利用的小碎片。

**最差适应算法（WF）**：选择最大的空闲分区。这样分割后剩余的空间仍然较大，可以容纳其他进程，但大分区很快就被分割成中等大小的分区，导致大进程无法分配。

## 三、分页式存储管理

分页是解决内存碎片问题的有效方法。它将物理内存划分为大小相等的块，称为"页框"或"帧"（Frame），将进程的逻辑地址空间也划分为同样大小的块，称为"页"（Page）。进程的各个页可以分散存放在不连续的页框中，通过"页表"来记录页与页框的映射关系。

### 3.1 地址变换过程

逻辑地址由两部分组成：[页号 | 页内偏移地址]。地址变换的过程是：

**第一步**：从逻辑地址中提取页号和页内偏移。这通过简单的除法和取模运算完成。
**第二步**：以页号为索引在页表中查找对应的页框号。
**第三步**：计算物理地址 = 页框号 × 页面大小 + 页内偏移。

**例题**：假设页面大小为4KB，页表内容为 {0→2, 1→4, 2→6, 3→8}，求逻辑地址4100对应的物理地址。

> 页面大小 = 4KB = 4096B
> 页号 = 4100 / 4096 = 1（商）
> 页内偏移 = 4100 mod 4096 = 4（余数）
> 页表中页号1对应的页框号为4
> 物理地址 = 4 × 4096 + 4 = 16384 + 4 = 16388

### 3.2 快表（TLB）

由于页表存放在内存中，每次地址变换需要额外的一次内存访问（查页表），导致实际访存时间翻倍。TLB（快表）就是用来解决这个问题的。

TLB是一个容量很小但速度极快的硬件缓存，它存储了最近使用的页表项。在进行地址变换时，CPU首先检查TLB：

- 如果TLB命中（所需页号在TLB中），则直接得到页框号，无需访问内存中的页表
- 如果TLB未命中，则需要访问内存中的页表，并将新的映射关系加入TLB

**TLB访问时间计算**：
假设TLB访问时间为1ns，内存访问时间为100ns，TLB命中率为98%。则平均页面访问时间为：
> 平均时间 = 0.98 × (1 + 100) + 0.02 × (1 + 100 + 100) = 98.98 + 4.02 = 103ns
>
> 对比不使用TLB的情况：100 + 100 = 200ns。TLB将平均访问时间减少了近一半。

## 四、分段式存储管理

分段（Segmentation）是另一种存储管理方式。它从用户编程的角度出发，将一个程序划分为多个逻辑段：代码段、数据段、堆栈段等。每个段有独立的逻辑地址空间，从0开始编址。

**逻辑地址结构**：[段号 | 段内偏移]

**与分页的关键区别**：
1. 分页是系统视角（为了管理便利），分段是用户视角（为了编程便利）
2. 页面大小固定，段大小可变
3. 分页是"看不见"的（对程序员透明），分段是"看得见"的（程序员需要感知段的存在）
4. 分段便于实现共享和保护（可以为每个段设置独立的访问权限）

## 五、段页式存储管理

段页式结合了分段和分页的优点：先将程序按逻辑分段，再将每个段分成固定大小的页。地址变换需要两次查表：先查段表得到页表起始地址，再查页表得到页框号。虽然速度慢了一些，但兼顾了分段的逻辑清晰性和分页的存储效率。
'''


def os_06():
    return f'''# OS-06 虚拟存储器

## 一、虚拟存储的基本原理

虚拟存储器的核心思想是：程序在运行时不需要全部装入内存，只需要装入当前需要使用的部分。这基于一个重要的观察——**程序的局部性原理**。

**时间局部性**：刚被访问过的地址很可能在不久的将来再次被访问。这是因为程序中有大量的循环结构。

**空间局部性**：刚被访问过的地址附近的地址也很可能被访问。这是因为程序中的代码和数据通常顺序存放、顺序执行。

基于局部性原理，操作系统可以在物理内存有限的情况下运行比内存大得多的程序：只将当前需要的页面装入内存，其余的保持在磁盘上。当程序访问的页面不在内存中时，操作系统负责将其从磁盘调入。这就是"按需调页"技术。

## 二、缺页中断处理过程

当CPU访问的页面不在物理内存中时，MMU（内存管理单元）会触发一个缺页异常（Page Fault），操作系统捕获这个异常后进行以下处理：

1. **保护现场**：保存当前进程的CPU寄存器状态
2. **检查页表**：确认该页面确实在磁盘上（而非非法访问）
3. **查找空闲页框**：如果内存中有空闲页框，直接使用；如果没有，需要选择一个页面置换出去
4. **启动磁盘I/O**：从磁盘读取所需的页面到页框中
5. **更新页表**：添加新的页号→页框号映射关系
6. **恢复现场**：重新执行引起缺页的指令

这个过程中，步骤4是需要大量时间的（磁盘I/O通常在毫秒级，而CPU操作在纳秒级，相差百万倍）。

## 三、页面置换算法

当内存没有空闲页框时，必须选择一个已经存在的页面置换出去，为新页面腾出空间。不同的置换策略对系统的性能影响很大。

{SVG_REPLACE}

### 3.1 FIFO（先进先出）

FIFO选择最先进入内存的页面进行置换。它用一个队列来维护页面进入的顺序，每次淘汰队首的页面。

**优点**：实现简单，只需要维护一个队列。

**缺点**：没有考虑页面的使用频率。一个被频繁访问的页面可能因为"来得早"而被置换出去，导致刚被置换又立刻需要调回来。更糟糕的是，FIFO存在**Belady异常**——增加可用页框数反而会导致缺页率上升。

### 3.2 LRU（最近最久未使用）

LRU选择最长时间没有被访问的页面进行置换。它基于一个合理的假设：如果一个页面很久没被访问过，那么它在未来被访问的可能性也比较小。

**优点**：性能接近理论最优的OPT算法，没有Belady异常。

**缺点**：实现成本较高，需要记录每个页面的最后访问时间，或者维护一个按访问时间排序的链表。

### 3.3 OPT（最佳置换）

OPT置换未来最长时间内不会被访问的页面。它需要"预知未来"——知道进程未来的页面访问序列。

**优点**：理论上缺页率最低。

**缺点**：无法在实际系统中实现，只能作为衡量其他算法性能的参考标准。通常用于教学和算法评估。

### 3.4 置换过程详解

以引用串 7,0,1,2,0,3,0,4,2,3,0,3,2 为例，3块物理帧：

**FIFO置换过程**：
```
7 → [7]           缺页
0 → [7,0]         缺页
1 → [7,0,1]       缺页
2 → [0,1,2]       缺页，7被置换（最早进入）
0 → [0,1,2]       命中
3 → [1,2,3]       缺页，0被置换
0 → [2,3,0]       缺页，1被置换
4 → [3,0,4]       缺页，2被置换
2 → [0,4,2]       缺页，3被置换
3 → [4,2,3]       缺页，0被置换
0 → [2,3,0]       缺页，4被置换
3 → [2,3,0]       命中
2 → [2,3,0]       命中
缺页9次，缺页率9/13≈69%
```

**LRU置换过程**：
```
7 → [7]           缺页
0 → [7,0]         缺页
1 → [7,0,1]       缺页
2 → [0,1,2]       缺页（7是最久未用）
0 → [1,2,0]       命中（0被提到最近）
3 → [2,0,3]       缺页（1最久未用）
0 → [2,3,0]       命中
4 → [3,0,4]       缺页（2最久未用）
2 → [0,4,2]       缺页（3最久未用）
3 → [4,2,3]       缺页（0最久未用）
0 → [2,3,0]       缺页（4最久未用）
3 → [2,0,3]       命中
2 → [0,3,2]       命中
缺页6次，缺页率6/13≈46%
```

## 四、影响缺页率的因素

1. **页面大小**：页面越大，页表占用越小，但每次缺页传输的数据量也越大，且可能造成更多的内部碎片
2. **进程分配的物理块数**：块数越多，缺页率越低（但不是线性关系）
3. **页面置换算法**：OPT > LRU > FIFO（按缺页率升序排列）
4. **程序的局部性**：局部性好的程序（如矩阵按行访问）缺页率显著低于局部性差的程序（如矩阵按列访问）

## 五、系统颠簸（Thrashing）

颠簸是虚拟存储系统中一种最糟糕的状态。它的发生过程是：

1. 进程频繁缺页，不断发出磁盘I/O请求
2. CPU大部分时间在等待磁盘I/O，利用率下降
3. 操作系统误以为"CPU不够用"，引入更多进程
4. 更多进程竞争有限的内存，每个进程的可用页框更少
5. 缺页更加频繁，CPU利用率进一步下降

这是一个恶性循环，最终导致系统瘫痪——CPU利用率接近0%，而磁盘持续繁忙。

**改进方法**：采用工作集模型，为每个进程分配足够的工作集（当前正在使用的那部分页面），使缺页率保持在可接受的水平。
'''


def os_07():
    return '''# OS-07 输入输出系统

## 一、I/O控制方式的演进

I/O控制方式经历了从CPU完全参与到几乎不需CPU参与的发展过程，每一种新方式都是为了解决前一种方式的性能瓶颈。

### 1.1 轮询（程序直接控制）

这是最原始的I/O方式。CPU不断读取设备的状态寄存器，检查设备是否准备好了。如果没准备好，CPU就空转等待。

**形象理解**：就像你在水壶前一直盯着，问"水开了吗？""水开了吗？"——CPU什么其他事都不能做。

**缺点**：CPU利用率极低，因为CPU的速度远快于I/O设备。

### 1.2 中断驱动

CPU向设备发出I/O请求后，不再等待，而是继续执行其他程序。当设备完成操作时，通过中断信号通知CPU。

**形象理解**：把水壶放在炉子上，设个闹钟，然后去做其他事。闹钟响了再去关火。

**优点**：CPU可以在I/O等待期间执行其他程序，利用率大幅提高。

**缺点**：对于每个字节的传输都需要一次中断，大量中断导致CPU频繁切换。

### 1.3 DMA（直接内存访问）

DMA控制器是一个专门的硬件，它可以直接在设备和内存之间传输数据，不需要CPU逐一处理每个字节。CPU只需设置好传输参数（源地址、目标地址、数据长度），然后DMA控制器独立完成数据传输，完成后发一个中断通知CPU。

**典型应用**：磁盘读写。读取一个4KB的磁盘块，如果使用中断驱动，需要产生4096次中断；使用DMA只需要1次中断。

### 1.4 通道

通道是一种更高级的I/O控制方式，它实际上是一个专门处理I/O操作的处理器。通道有自己的指令集，可以执行"通道程序"来自主完成复杂的I/O操作序列。

## 二、I/O子系统层次结构

操作系统中的I/O系统采用分层设计，每一层屏蔽下层的细节，为上层提供抽象服务。

**用户层软件**：应用程序通过系统调用接口（如file.read()、socket.send()）发起I/O请求。

**设备独立性软件**：这一层实现了设备无关性。应用程序使用逻辑设备名（如/dev/lp0）访问设备，由这一层映射为具体的物理设备。同时，这一层还负责设备命名、保护、缓冲和错误处理。

**设备驱动程序**：每个设备类型都有对应的驱动程序。驱动程序理解设备硬件的特定命令格式，将上层的通用I/O请求转换为设备能理解的命令序列。

**中断处理程序**：处理设备完成I/O后发起的中断，唤醒等待I/O完成的进程。

## 三、SPOOLing技术

SPOOLing（假脱机操作）是一种将独占设备虚拟化为共享设备的技术。最典型的应用是打印机共享。

在没有SPOOLing的情况下，打印机一次只能为一个进程服务。有了SPOOLing，情况就不同了：

**SPOOLing系统的组成**：
- **输入井**和**输出井**：磁盘上开辟的缓冲区域，用于暂存I/O数据
- **输入进程**和**输出进程**：负责数据在缓冲区和设备之间的传输
- **井管理程序**：控制缓冲区资源的使用

**工作流程**：多个进程同时提交打印任务 → 每个任务被写入磁盘的输出井中排队 → 打印机空闲时从输出井取出任务依次打印。从进程的角度看，打印机"看起来"是同时可用的——每个进程都感觉自己独占打印机。

**核心思想**：用磁盘这个共享设备 + 缓冲技术，将独占设备改造成"虚拟共享设备"。
'''


def os_08():
    return '''# OS-08 文件管理

## 一、文件系统的基本功能

文件系统是操作系统中管理持久化数据的子系统。它负责将用户的数据以一种有结构的方式组织在磁盘上，并提供高效、安全的访问接口。

文件系统的主要功能包括：
1. 提供用户友好的文件操作接口（创建、打开、读写、关闭、删除）
2. 管理文件的存储空间（分配和回收磁盘块）
3. 实现文件的共享和保护（多用户环境下）
4. 保证文件访问的效率和安全性

## 二、文件目录结构

文件目录是文件系统组织文件的形式。不同的目录结构各有优劣。

**单级目录**：所有文件都在一个目录中。好比把所有文件都放在桌面上——在小规模时很简洁，但文件一多就混乱不堪。此外，不同用户的文件可能重名，造成冲突。

**二级目录**：为每个用户建立一个独立的子目录。这样不同用户的文件可以重名，查找效率也有所提升。但用户内部的文件组织仍然是扁平的。

**树形目录**：最常用的结构。用户可以在自己的目录下创建任意层级的子目录来组织文件。这是现代操作系统通用的方案，Linux和Windows都使用树形目录。树形目录的优势在于：层次清晰、支持按类别组织文件、查找效率高。

## 三、文件共享

**硬链接**：多个文件名指向同一个索引节点（inode）。在Linux中，硬链接通过ln命令创建。硬链接的特点是所有链接是"平等"的——删除其中一个，不影响其他，只有当文件的链接数降为0时，文件数据才被真正删除。

**软链接（符号链接）**：链接文件本身是一个特殊文件，它存储的是目标文件的路径名。访问链接文件时，操作系统自动重定向到目标路径。软链接类似于Windows的快捷方式。它的特点是：可以跨文件系统，但目标文件被删除后链接失效。

## 四、文件保护

**口令保护**：用户在创建文件时设置口令，访问时需验证口令。缺点是口令容易被泄露。

**加密保护**：文件数据经过加密后存储，只有持有解密密钥的用户才能读取。即使数据被窃取，没有密钥也无法解密。

**访问控制（ACL）**：为每个文件维护一个访问控制列表，记录每个用户或用户组对该文件的权限（读、写、执行）。这是最灵活的保护方式，也是Unix/Linux系统采用的方式。

在Linux中，每个文件有三组权限（所有者、同组用户、其他用户），每组权限由读(r)、写(w)、执行(x)三个二进制位组成。例如权限"rwxr-xr--"表示：所有者可读写执行，同组用户可读执行，其他用户只读。
'''


def os_09():
    return '''# OS-09 磁盘存储器管理

## 一、外存分配方式

磁盘上的文件如何存放？不同的分配方式对文件访问的性能和磁盘空间的利用率有直接影响。

**连续分配**：为每个文件分配一组连续的磁盘块。就像在图书馆里把一本书的每一页按顺序放在相邻的书架上——读起来很顺畅，但想插入新内容就麻烦了。连续分配支持高效的顺序访问和直接访问，但会产生外部碎片（类似内存管理中的碎片问题），且文件扩展困难。

**链接分配**：文件的每个数据块中包含指向下一个数据块的指针，形成一个链表。这种方式没有外部碎片，文件可以随需扩展。但缺点是只能顺序访问，无法直接访问文件的中间部分。链接分配又分为**隐式链接**（指针存储在数据块中，用户不可见）和**显式链接**（使用文件分配表FAT集中管理链接信息）。

**索引分配**：为每个文件建立一个索引块，索引块中存放文件所有数据块的地址列表。这种方案既支持高效的随机访问，又没有外部碎片，是最灵活的分配方式。缺点是引入了索引块的开销。

## 二、文件分配表（FAT）计算

FAT表采用的显式链接的变体。整个磁盘只有一个FAT表，其中每个条目对应一个磁盘块，存储的是该块的下一个块号。

**例题**：磁盘分区大小为4GB，磁盘块大小为4KB，问FAT表至少需要多少位宽？FAT表占多少空间？

> 磁盘块总数 = 4GB / 4KB = 2^32 / 2^12 = 2^20 = 1,048,576 块
> FAT表位宽 = ⌈log₂(1,048,576)⌉ = 20位
> FAT表大小 = 1,048,576 × 20 / 8 ≈ 2.5MB

## 三、混合索引结构

混合索引是Unix/Linux采用的外存分配方式。它结合了直接索引和多重间接索引的优点。

**以Unix为例（块大小1KB，地址项4B）**：
- 直接索引：10个直接地址项，覆盖0-9号数据块
- 一级间接索引：1个间接地址项，覆盖10-265号数据块
- 二级间接索引：1个二级间接项，覆盖266-65793号数据块
- 三级间接索引：1个三级间接项，覆盖更大范围

**访盘次数计算**：访问一个磁盘块需要几次磁盘I/O？
- 直接索引中的块：1次（直接读数据块）
- 一级间接索引中的块：2次（读间接索引块→读数据块）
- 二级间接索引中的块：3次（读二级间接块→读一级间接块→读数据块）

**例题**：块大小1KB，地址项4B，直接索引10项，一级/二级/三级间接各1项，求最大文件长度。

> 每块可存储地址数 = 1024/4 = 256个
> 直接索引容量 = 10 × 1KB = 10KB
> 一级间接容量 = 256 × 1KB = 256KB
> 二级间接容量 = 256 × 256 × 1KB = 64MB
> 三级间接容量 = 256 × 256 × 256 × 1KB = 16GB
> 最大文件长度 ≈ 16.06GB

## 四、磁盘调度算法

磁盘的I/O速度远慢于内存和CPU，因此磁盘调度的目标是最小化磁头的移动距离——特别是寻道时间。

**先来先服务（FCFS）**：按请求到达顺序依次服务。公平但效率很低，磁头可能在一个很大的范围内来回移动。

**最短寻道时间优先（SSTF）**：每次选择离当前磁头最近的请求处理。效率比FCFS高，但可能导致远处的请求长时间得不到服务（饥饿现象）。

**电梯算法（LOOK）**：磁头沿一个方向移动，处理沿途的请求，到达该方向的最远请求点后反向。类似于电梯的运行方式——在上行途中只处理上行请求。

**循环扫描算法（C-LOOK）**：单向移动，到达最远端后直接跳回最近端（不处理反向请求）。这样保证了所有磁道有相对均匀的等待时间。

**例题**：请求队列 [98, 183, 37, 122, 14, 124, 65, 67]，当前磁头位置53，用SSTF计算平均寻道长度。

> 当前53，最近的是65（距离12），然后是67（2），37（30），14（23），98（84），122（24），124（2），183（59）
> 移动路径：53→65→67→37→14→98→122→124→183
> 总寻道长度：12+2+30+23+84+24+2+59 = 236
> 平均寻道长度 = 236/8 = 29.5
'''

print("=== Generating OS v2 notes ===")
notes = [
    ('OS-00-目录与总览.md', os_00()),
    ('OS-01-操作系统引论.md', os_01()),
    ('OS-02-进程描述与控制.md', os_02()),
    ('OS-03-处理机调度.md', os_03()),
    ('OS-04-进程同步与死锁.md', os_04()),
    ('OS-05-存储器管理.md', os_05()),
    ('OS-06-虚拟存储器.md', os_06()),
    ('OS-07-输入输出系统.md', os_07()),
    ('OS-08-文件管理.md', os_08()),
    ('OS-09-磁盘存储器管理.md', os_09()),
]

for fn, content in notes:
    content = content.replace('__SVG_BANKER__', SVG_BANKER)
    w(fn, content.lstrip('\n'))

import re
total = sum(len(re.findall(r'[一-鿿㐀-䶿]', c)) for _, c in notes)
print(f"\\nOS total Chinese chars: {total}")
