---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import StarParticles from '../components/StarParticles.astro';
import SplashScreen from '../components/SplashScreen.astro';

export interface Props {
  title?: string;
  description?: string;
  wide?: boolean;
  splash?: boolean;
}

const { title = 'Guorui', description = 'Personal site', wide = false, splash = false } = Astro.props;
const containerClass = wide ? 'container-wide' : 'container';
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} — Guorui</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    {splash && <SplashScreen />}
    <StarParticles />
    <Header />
    <main class="page">
      <div class={containerClass}>
        <slot />
      </div>
    </main>
    <Footer />
  </body>
</html>
